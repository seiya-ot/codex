## コードレビュー概要

### 良い点
- **関心の分離が明確**: `ManualCatalogStore`、`RequestResolver`、`ManualCrawler` といったサービスがそれぞれの責務をカプセル化しています。
- **非同期処理が徹底**: I/O バウンドの処理はすべて async で実装されており、スレッドプールのブロッキングを防いでいます。
- **スレッド安全**: `ManualCatalogStore` は `SemaphoreSlim` によりカタログのロード／リフレッシュを排他制御しています。
- **依存性注入**: すべてのサービスが最小 API ビルダーでシングルトンとして登録されており、テストやモックが容易です。
- **JSON 設定が統一**: CamelCase のプロパティ命名とインデント設定がプロジェクト全体で一貫しています。
- **堅牢なパース**: `RequestResolver` の正規表現ヘルパーはメソッド・パスのヒントやクエリ項目抽出を包括的に処理しています。

### 改善点
1. **カタログリフレッシュ時のエラーハンドリング**
   - `RefreshInternalAsync` は `ManualCrawler.BuildCatalogAsync` やファイル書き込みで例外が発生した場合に捕捉していません。失敗するとストアが不整合状態になる可能性があります。try/catch で例外を捕捉し、ログ出力とともに以前の `Current` を保持するようにすると安全です。
2. **キャンセルトークンの伝搬**
   - `RefreshInternalAsync` は `CancellationToken` を受け取りますが、`File.Create` や `JsonSerializer.SerializeAsync` には渡していません。トークンを伝搬させることで、処理の途中で安全にキャンセルできます。
3. **メモリ使用量の可能性**
   - `ManualCrawler.BuildCatalogAsync` は全ページをメモリに読み込んでからカタログを構築します。非常に大規模なマニュアルではメモリ負荷が高くなる恐れがあります。ページをストリーム処理したり、インクリメンタルに加工する方式に変更すると改善できます。
4. **候補理由の重複処理**
   - `ResolvedCandidate.Reasons` は `Distinct` で重複除去していますが、最初のリスト生成時に同様の文字列が多数追加され、余計なメモリ確保が発生します。`HashSet<string>` を使って最初から重複を排除すれば効率的です。
5. **スコアリングのマジックナンバー**
   - スコア計算に使用されている定数（例: `120`、`1_000`、`250`）がハードコードされており意味が分かりにくいです。名前付き定数や設定クラスに切り出すことで可読性と調整容易性が向上します。
6. **正規表現のパフォーマンス**
   - `Regex.Match` 系の呼び出しが頻繁に行われ、`Compiled` オプションが付与されていません。高スループットが要求される場面では静的にコンパイル済み正規表現を利用すると速度が改善します。
7. **ロギングの欠如**
   - 現在アプリケーションにロギングが実装されていません。`ILogger<T>` を注入し、特に `ManualCrawler` のネットワーク失敗時などにログを残すことでトラブルシューティングが容易になります。

### マイナー提案
- パブリッククラス・メソッドに XML ドキュメントコメントを追加し、IntelliSense を充実させましょう。
- 単純なプロパティ getter は式形式メンバー (`private string CatalogDirectory => Path.Combine(...);`) にするとコードがすっきりします。
- 型が明らかな場合は `var` を積極的に使い、冗長さを減らしましょう。
- `RequestResolver.ParsePathHint` で抽出したパスは正規化（末尾スラッシュの統一など）を行うと、パス比較のミスマッチを防げます。

### アクション項目
- [ ] カタログリフレッシュを try/catch でラップし、適切なロギングとフォールバック処理を追加する。
- [ ] キャンセルトークンをファイル I/O メソッドへ伝搬させる。
- [ ] スコアリング定数を静的クラスに切り出す。
- [ ] コンパイル済み正規表現インスタンスを導入する。
- [ ] サービス層に基本的なロギング (`ILogger<T>`) を組み込む。

*以上でレビューを終わります。*